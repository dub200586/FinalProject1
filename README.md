# Transaction Report System

Система для обработки банковских транзакций, генерации отчетов и сохранения в базу данных PostgreSQL.

## 🚀 Функциональность

- **Чтение транзакций** из текстовых файлов
- **Валидация данных** транзакций
- **Генерация отчетов** в формате TXT
- **Сохранение в БД** PostgreSQL
- **Фильтрация отчетов** по датам
- **Поиск и управление** отчетами

## 🛠 Технологии

- Java 8+
- PostgreSQL
- JDBC
- File I/O Operations

## 📁 Структура проекта

FinalProject1/
├── src/
│ ├── Model/
│ │ ├── Transaction.java
│ │ └── TransactionReport.java
│ ├── Service/
│ │ ├── FileService.java
│ │ ├── ReportService.java
│ │ └── TransactionService.java
│ ├── Repository/
│ │ ├── ReportRepository.java
│ │ └── TransactionReportRepository.java
│ ├── Exception/
│ │ ├── FileProcessingException.java
│ │ ├── InvalidInputException.java
│ │ └── ReportNotFoundException.java
│ └── Main.java
├── lib/
│ └── postgresql-42.7.8.jar
├── input/
│ └── (файлы транзакций .txt)
└── output/

## ⚙️ Настройка базы данных

### 1. Установите PostgreSQL
- Скачайте с [официального сайта](https://www.postgresql.org/download/)
- Запомните логин/пароль при установке

### 2. Создайте базу данных
     sql
CREATE DATABASE transaction_db;

### 3. Настройте подключение в коде
В классе Main.java укажите ваши параметры:
private final static String DB_URL = "jdbc:postgresql://localhost:5432/transaction_db";
private final static String DB_USERNAME = "your_username";
private final static String DB_PASSWORD = "your_password";

## 🏃 Запуск проекта

Способ 1: Через IDE (IntelliJ IDEA/Eclipse)
Импортируйте проект в IDE

Добавьте postgresql-42.7.8.jar в classpath

Запустите Main.java

Способ 2: Через командную строку
# Компиляция
javac -cp ".;lib/postgresql-42.7.8.jar" -d out src/*.java

# Запуск
java -cp "out;lib/postgresql-42.7.8.jar" Main

## 📊 Формат входных файлов

Разместите файлы транзакций в папке input/ в формате:

Номер счета отправителя
Номер счета получателя
Сумма перевода

## 🎮 Использование

При запуске программы доступны команды:

1. Сохранить отчет по всем транзакциям
Читает все файлы из input/

Валидирует данные

Сохраняет отчет в БД и файл

2. Показать все отчеты
Отображает все сохраненные отчеты

Поддерживает фильтрацию по датам

## 🗃 Модель данных

Таблица: transaction_reports

sql
CREATE TABLE transaction_reports (
    id BIGSERIAL PRIMARY KEY,
    transaction_name VARCHAR(255) NOT NULL,
    is_successful BOOLEAN NOT NULL,
    state_text TEXT,
    operation_datetime TIMESTAMP NOT NULL,
    account_number_from VARCHAR(20),
    account_number_to VARCHAR(20),
    transfer_amount DECIMAL(15,2) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

## 🔧 Классы и их ответственность

Модели
Transaction - данные транзакции

TransactionReport - отчет по транзакции

Сервисы
FileService - работа с файловой системой

TransactionService - обработка транзакций

ReportService - управление отчетами

Репозиторий
TransactionReportRepository - работа с БД PostgreSQL

Исключения
FileProcessingException - ошибки обработки файлов

InvalidInputException - некорректный ввод

ReportNotFoundException - отчет не найден

🐛 Решение проблем

Ошибка: "No suitable driver found"
# Убедитесь, что JAR драйвер добавлен в classpath
java -cp ".;lib/postgresql-42.7.8.jar" Main

Ошибка: "Connection refused"
Проверьте, что PostgreSQL запущен

Убедитесь в правильности порта (по умолчанию 5432)

Проверьте логин/пароль

Ошибка: "Table doesn't exist"
Программа автоматически создает таблицу при первом запуске

Проверьте права доступа к БД

## 📝 Пример работы

Введите номер команды:
1
Обработанные файлы: директория: ./input/
Введите абсолютный путь сохранения отчета:
C:/reports
Отчет успешно создан: C:/reports\TransactionReport.txt

Введите номер команды:
2
Хотите ли отсортировать данные по датам?
да
Дата начала сортировки (чч-мм-ГГГГ): 01-01-2024
Дата окончания сортировки (чч-мм-ГГГГ): 31-12-2024

## 👥 Разработка

Для добавления новой функциональности:

Создайте класс в соответствующем пакете

Добавьте обработку в Main.java

Протестируйте работу
